  // Validate inputs
    if (limit == null || limit < 0 || offset == null || offset < 0 || lookUpType == null || lookUpType.isEmpty()) {
        throw new IllegalArgumentException("Invalid input parameters");
    }