SELECT
    DATE(line_Item_usage_start_date) AS usage_date,
    rt_user_product,
    SUM(line_item_unblended_cost) AS total_cost
FROM
    your_table_name
GROUP BY
    DATE(line_Item_usage_start_date),
    rt_user_product
ORDER BY
    usage_date, rt_user_product;
